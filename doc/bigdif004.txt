diff --git a/src/studio/pettil-dictionary.a65 b/src/studio/pettil-dictionary.a65
index a87b7b8..be00718 100644
--- a/src/studio/pettil-dictionary.a65
+++ b/src/studio/pettil-dictionary.a65
@@ -23,16 +23,17 @@ symtabset99

 ;--------------------------------------------------------------
 #if 0
-name=>SYMS
+name=SYM0
 stack=( -- nfa )
 tags=interpreter,ext,nosymbol
 Returns `nfa` of first symbol in the `symbol table`

 #endif
-#include "align.i65"
-tosyms
+sym0
     lda #34
     ldy usersymtab+1
+    sta R0L+2*N3
+    sty R0L+2*N3+1
     jmp pushya

 ;--------------------------------------------------------------
@@ -77,7 +78,6 @@ effects=A:len;Y:0;C:vocab?;Z:len
 tags=nfa,nosymbol
 6502registers=A:symlen Y:0 C:vocab
 Sweet16registers=N3:nfa N8:length
-return the length of the current `nfa` stored in `n3`

 #endif
 symlens
@@ -106,7 +106,6 @@ tags=ext,primitive,nfa,nosymbol
 * copy length/flags
 * does not move N7, does not move N3 either

-~evaluate
 #endif
 symcopy
     lda #>(next-1)
@@ -364,7 +363,7 @@ Populates `n3` with the current `nfa`

 ```
 : >name   ( cfa -- nfa|0 )
-    >syms >n3 drop
+    sym0 >n3 drop
     begin
         name> over <>
         sym.len and
@@ -378,7 +377,7 @@ Populates `n3` with the current `nfa`
 _toname
     jsr enter
 #include "page.i65"
-    .word symrewind
+    .word sym0
 #include "page.i65"
     .word ptoname
 #include "past.i65"
@@ -1002,7 +1001,7 @@ if `current` is nonzero, join this to the vocabulary
     bloom                       ( symtail 2+ )
     current
     2swap over cfa,
-    >syms here 3- ((create))
+    sym0 here 3- ((create))
     3coff dup smudge newest !
     false (seekb)
     if
@@ -1773,8 +1772,8 @@ _pforgetpass2
     .word branch
     .byt <(pforgetpass1a-*+1)

-;--------------------------------------------------------------
-#if 0
+/**************************************************************
+; #if 0
 name=(FORGET)
 stack=( cfa -- )
 tags=nfa,ext,nosymbol
@@ -1827,7 +1826,7 @@ SYMTAB+32 and ending at the triple null where SYMTAIL points.

     passbits $80 !+ off
     (forget).pass1 (filter)!
-    32 0  >syms pad
+    32 0  sym0 pad
     (forget).looper             \ does `ext-until` 32x, completing pass1

     passbits on
@@ -1835,7 +1834,7 @@ SYMTAB+32 and ending at the triple null where SYMTAIL points.
     16 0  pad 3+ (automem)
     (forget).looper             \ does `ext-until` 16x, completing pass2
 ```
-#endif
+; #endif
 #include "align.i65"
 _pforget
     jsr enter
@@ -1907,6 +1906,7 @@ _pforget
     .word symtailstore
 #include "past.i65"
     .word exit
+**************************************************************/

 ;--------------------------------------------------------------
 #if 0
@@ -1945,7 +1945,7 @@ address will be calculated at halftime.  After the second phase, the `symnew`

     passbits $80 !+ off
     (forget).pass1 (filter)!
-    32 0  >syms pad
+    32 0  sym0 pad
     (forget).looper             \ does `ext-until` 32x, completing pass1

     passbits on
@@ -1983,7 +1983,7 @@ _punremember
 ;#include "page.i65"
 ;    .word zero                 ; 32 0 ) \ factored into `(forget).looper`
 #include "page.i65"
-    .word tosyms
+    .word sym0
 #include "page.i65"
     .word _pad
 #include "page.i65"
@@ -2095,8 +2095,7 @@ _rehash
 #include "past.i65"
     .word exit

-;--------------------------------------------------------------
-#if 0
+/**************************************************************
 name=FORGET
 stack=( "name" -- )
 tags=interpreter,dictionary,forth-83,forth-79,fig
@@ -2112,7 +2111,7 @@ checks FENCE, sorts symbol table, keeping only words below the CFA of the named
     '  dup  fence @ studio @ between 0=
     7 ?error (forget) ;
 ```
-#endif
+
 #include "align.i65"
 _forget
     jsr enter
@@ -2133,6 +2132,7 @@ _forget
     .word _pforget
 #include "past.i65"
     .word exit
+ **************************************************************/

 ;--------------------------------------------------------------
 #if 0
diff --git a/src/studio/pettil-utils.a65 b/src/studio/pettil-utils.a65
index 1b7af56..38f245b 100644
--- a/src/studio/pettil-utils.a65
+++ b/src/studio/pettil-utils.a65
@@ -512,17 +512,8 @@ dup c@ $40 and 0=               \ TRUE if in core


 ```
-: wordlist   ( voc -- )
-    sym.rewind
-    begin
-        (voc) over =
-        ?terminal 0= and
-        if  ?cr id.l  then
-        3 sym+ 0=
-    until drop ;
-
 : word-list   ( voc -- )
-    sym.rewind
+    sym0
     begin
         (voc) over =
         if id.wl then
