; Assembler workaround for $xxFF bug.  Use in secondaries between
; the header #endif and the label, like this:
;
; #if 0
; name=...
; ...
; header
; ...
; #endif
; #INCLUDE "ALIGN.I65"
; _secondarylabel
;	  jsr enter
; #include "page.i65"
;     .word exit

; ensure at least 2 bytes after JSR ENTER
#if ((* & $ff)=$fc)
    nop
#endif
