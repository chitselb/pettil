tags: static
title: Editor Design

This is the editor design document

PETTIL leverages the PET's built-in ROM screen editor and uses the `stop` key like an escape key to prefix [[commands|EditorCommands]].