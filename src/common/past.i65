; pettil/src/common/past.i65
;
; this include goes at the end of a definition, where
#if ((* & $ff)=$ff)
#print *
#echo past?
    .byt $92                    ; PETTIL is expecting a CFA.  We should put
                                ; something here that sends `next` to $92xx,
                                ; an area which reads as the $92 JAM opcode
#endif
